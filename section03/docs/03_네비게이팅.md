# 3. 네비게이팅
- Next.js 앱의 페이지 이동을 의미
- Next.js의 사전 렌더링(Pre-rendering)
  - React App의 단점 해소(빠른 FCP 달성) + React App의 장점 승계(빠른 페이지 이동)

- `<a>태그`
  - 우리가 보통 HTML로 코드를 작성할 때 다른 페이지로 이동하는 링크를 만들고 싶을 때 사용
  - 클라이언트 사이드 렌더링(CSR)방식으로 페이지를 이동 X
  - 서버에게 새로운 페이지를 매번 다시 요청하는 방식으로 페이지를 이동 → `느린 방식`

### ① &lt;Link&gt;  컴포넌트
```tsx
import Link from 'next/link';

export default function Page() {
  return (
    ...
    <header>
      <Link href={'/'}>index<Link>
      <Link href={'/search'}>search<Link>
      <Link href={'/book/1'}>book/1<Link>
    </header>
    ...
  );
}
```

### ② 프로그래매틱한 페이지 이동(Programmatic Navigation)
- 사용자가 링크를 직접 클릭했을 때, 페이지를 이동시키는 방식X
- 특정 버튼이 클릭이 되었거나 특정 조건이 만족했을 경우 어떠한 `함수 내부`에서 페이지를 이동시키는 방법
- 예) 버튼을 클릭했을 때 이벤트 핸들러 함수 안에서 페이지를 이동시키는 기능
  ```tsx
  import Link from 'next/link';
  import [ useRouter ] from 'next/router';
  // 앱의 모든 함수 컴포넌트 내에서 router Object에 액세스하려면 useRouter 훅을 사용할 수 있다.

  export default function Page() {
    const router = useRouter();

    const onClickButton = () => {
      router.push('/test');
      // router 객체의 push메서드를 실행
    };

    return (
      ...
      <header>
        <Link href={'/'}>index<Link>
        <Link href={'/search'}>search<Link>
        <Link href={'/book/1'}>book/1<Link>
        <div>
          <button type="button" onClick={onClickButton}>/test 페이지로 이동</button>
        </div>
      </header>
      ...
    );
  }
  ```
  ```
  📌 참고
  ※ Router (https://nextjs-ko.org/docs/pages/api-reference/functions/use-router#routerpush)
  - router.push
    : 클라이언트 측 전환을 처리 
    → router.push(url, as, options)
    단, 외부 URL에는 router.push를 사용할 필요 X → window.location(opens in a new tab)이 더 적합
  - router.replace
    : router.replace는 history 스택에 새 URL 항목을 추가하지 않음 
    → router.replace(url, as, options)
  - router.back
    : 히스토리에서 뒤로 이동, 브라우저의 뒤로 가기 버튼을 클릭하는 것과 동일, window.history.back()을 실행
    → router.back()
  ```
- 즉, 컴포넌트 내부에서 특정 조건이 만족했거나 useEffect를 통해서 어떠한 상황을 가정한다거나 했을 때 함수 내부에서도 페이지를 CSR방식으로 이동시킬 수 있다.