# 7. 글로벌 레이아웃 설정하기

- Next.js 앱의 모든 페이지의 부모 컴포넌트 역할을 하는 `루트 컴포넌트`인 바로 `App컴포넌트(_app.tsx)`의 레이아웃을 적용
- 예&#41; 모든 페이지에 다 적용이 되는(`글로벌 레이아웃`) header와 footer 생성
```tsx
// /src/pages/_app.tsx

import "@/styles/globals.css";
import type { AppProps } from "next/app";

export default function App({ Component, pageProps }: AppProps) {
  return (
    <div>
      <header>상단 헤더</header>
      <main>
        <Component {...pageProps} />
      </main>
      <footer>하단 푸터</footer>
    </div>
  )
}
```

- 예&#41; 글로벌 레이아웃을 위한 `별도의 컴포넌트를 파일`로 만들어서 코드를 분리
```tsx
// /src/components/global-layout.tsx

import Link from 'next/link'
import styles from "@/components/global-layout.module.css";

export default function GlobalLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className={styles.container}>
      <header className={styles.header}>
        <Link href="/">📚 ONEBITE BOOKS</Link>
      </header>
      <main className={styles.main}>{children}</main>
      // page역할을 하는 Component는 children으로 앱 컴포넌트로부터 전달을 받음
      <footer className={styles.footer}>제작 &copy;김현주</footer>
    </div>
  )
}
```
```tsx
// /src/pages/_app.tsx

import "@/styles/globals.css";
import type { AppProps } from "next/app";
import GlobalLayout from "@/components/global-layout";

export default function App({ Component, pageProps }: AppProps) {
  return (
    <GlobalLayout>
      <Component {...pageProps} />
    </GlobalLayout>
  )
}
```